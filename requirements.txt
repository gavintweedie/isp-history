# Runtime dependencies (pinned for reproducible deploys).
# The vendored frontend libs (cytoscape.js, dagre) live in app/static/vendor
# and are NOT pip packages.
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.1
MarkupSafe==3.0.3
blinker==1.9.0
